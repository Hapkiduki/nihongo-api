# Dockerfile for development environment with hot-reload using Air
# This builds a development image with Air installed for automatic code reloading.

FROM golang:1.25.1-alpine

WORKDIR /app

# Copy dependency files
COPY go.mod go.sum ./
RUN go mod download

# Copy source code
COPY . .

# Expose port
EXPOSE 3000

# Run with Air for hot-reload
CMD ["air", "-c", ".air.toml"]